const Header = () => {
  return `<header class="weather__header">
      <span class="weather__title">Předpověď počasí</span>
      <span class="weather__status"></span>
      </header>`;
};

export default Header;
